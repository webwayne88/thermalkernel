# ThermalKernel — правила разработки

Детерминированное расчётное ядро тепловой защиты зданий под СП 50.13330 (паттерн Trusted Kernel + LLM Conductor). **Этот прогон — только голое ядро (Этап 0–1), без LLM и без UI.**

Стек: Python 3.11+, стандартная библиотека + Pydantic v2. Никакого numpy (стационарный расчёт — это алгебра). Тесты: pytest, в `tests/`. Линт: ruff.

Архитектура (ядро):
- `thermalkernel/models.py` — Pydantic-модели: Material, Layer, Construction, Climate, CalcResult.
- `thermalkernel/calc.py` — чистые функции расчёта (R_слоя, R₀_усл, R₀_пр, ГСОП, R_норм, вердикт). Без сети, без LLM, без I/O.
- `thermalkernel/data.py` — загрузка JSON-справочников.
- `thermalkernel/data/*.json` — версионируемые справочники (λ материалов, климат, коэффициенты a/b, α, n).
- `thermalkernel/refs.py` — реестр ссылок на пункты СП (каждый вывод трассируется до пункта).
- `thermalkernel/cli.py` + `__main__.py` — запуск из консоли.

Доменные правила (ядро — оракул):
- ЯДРО НЕ ОБРАЩАЕТСЯ К СЕТИ И LLM. Чистые функции, детерминированные.
- Каждый расчётный результат снабжён ссылкой на пункт/формулу СП (поле `ref` / `sp_refs`).
- Формулы (раздел 6.1 паспорта): `R_слоя = δ/λ`; `R₀_усл = 1/α_в + ΣR + 1/α_н`; `ГСОП = (t_в − t_от)·z_от`; `R_норм = a·ГСОП + b`; условие соответствия `R₀_пр ≥ R_норм`.
- Никаких молчаливых дефолтов в нормативно значимых величинах: неполный/невалидный ввод (δ≤0, λ≤0, неизвестный материал/город/тип) → явная ошибка.
- Справочные значения — НАШИ структурированные данные со ссылкой на пункт-источник, НЕ дословная копия таблиц СП (правовая серая зона, раздел 7 паспорта). У каждой записи поле `source`.
- ВАЖНО: эталонов СП 23-101 в этом прогоне нет → тесты = ручные поверки/самосогласованность. В коде и README пометить, что коэффициенты и тест-кейсы требуют валидации экспертом и реальными примерами СП 23-101.

Стиль: простой читаемый код, минимум абстракций, без лишнего сахара. Комментарии к доменной логике — на русском. Имена идентификаторов — на английском (transliteration допустима для доменных терминов: r0_usl, gsop, r_norm).
